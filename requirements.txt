# Core Django
Django>=5.1,<5.2
django-environ
psycopg2-binary
django-extensions

# API
djangorestframework
djangorestframework-simplejwt
django-cors-headers

# Database & Cache
redis
django-redis
elasticsearch>=8.0.0
django-elasticsearch-dsl

# Async & Queue
celery
django-celery-results
django-celery-beat

# Authentication & Social
django-allauth
dj-rest-auth
django-two-factor-auth
django-otp

# Admin UI
django-jazzmin

# Frontend (TALL Stack equivalents)
django-htmx
django-compressor
sass

# Realtime
channels[daphne]
channels-redis

# Payments
stripe
payme-pkg  # Specific package if available or custom implementation

# AI Integration
openai
langchain
tiktoken

# File Storage
django-storages[boto3]
whitenoise

# Monitoring & Logging
sentry-sdk
django-prometheus

# Production
gunicorn
uvicorn
Pillow
python-magic
python-dotenv
py-ubjson ; sys_platform == "never"
